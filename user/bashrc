#!/bin/bash

# File for local unversioned requirements
fileBashrcLocal=~/.bashrc_local
if [ -e "$fileBashrcLocal" ]; then
  source $fileBashrcLocal
fi

# Bash config
source ~/dotfiles/shell-bash/prompt

# Run commands
source ~/dotfiles/runcom/.nvmrc
